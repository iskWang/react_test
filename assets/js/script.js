// Generated by CoffeeScript 1.9.3
(function() {
  var R, inputBox;

  R = React.DOM;

  inputBox = React.createClass({
    getInitialState: function() {
      return {
        value: 'hello'
      };
    },
    handleChange: function(event) {
      return this.setState({
        value: event.target.value
      });
    },
    render: function() {
      var i, value;
      value = this.state.value;
      return R.div(null, R.input({
        type: 'text',
        value: value,
        onChange: this.handleChange
      }), R.br(null), (function() {
        var j, ref, results;
        if (value.length > 0) {
          results = [];
          for (i = j = 0, ref = value.length - 1; 0 <= ref ? j <= ref : j >= ref; i = 0 <= ref ? ++j : --j) {
            results.push(R.button({
              className: 'btn btn-xs btn-default'
            }, value[i]));
          }
          return results;
        }
      })());
    }
  });

  React.render(React.createElement(inputBox, null), document.getElementById('content'));

}).call(this);
